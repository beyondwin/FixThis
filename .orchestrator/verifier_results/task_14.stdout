**Verification: FAIL**

- `CompactHandoffRendererTest`: 17/17 pass (2 new tests included).
- Full `:fixthis-mcp:test`: 5 failures = 2 baseline + **3 new regressions** caused by Task 2.1.

Root cause: Task 2.1 changed `CompactHandoffRenderer` to emit a `candidates:` block in place of the `src? ` line, but did not propagate the change to:

1. `FeedbackQueueFormatterTest.kt:542` & `:496` — assertions still expect the old `src? `/`src? unknown` form (these tests exercise `CompactHandoffRenderer` via `FeedbackQueueFormatter.toMarkdown`).
2. `fixthis-mcp/src/test/resources/parity/expected-prompt.txt:12` — fixture still has `src? AppPrimaryButton.kt:42 high; why=tag+compTag`.
3. `fixthis-mcp/src/main/resources/console/app.js:1850` — JS-side compact renderer still emits `'src? ' + location + ' ' + confidence`, **breaking JS/Kotlin parity** (the actual driver of `PromptParityTest`).

Also: `compactSourceLine` and `reasonTokenFor` in `CompactHandoffRenderer.kt:116-142` are now dead code, and the new candidates block drops the `why=…` tokens — confirm with spec whether per-candidate `why=` should still surface.

Result written to `.orchestrator/verifier_results/task_14.json`.

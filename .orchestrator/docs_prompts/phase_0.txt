You are a Phase Docs Updater sub-agent on Sonnet. Update top-level docs to reflect changes shipped in Phase 0 of the v2 plan. Do not change implementation files.

## Working-Directory Enforcement
- Worktree: `/Users/kws/source/android/worktrees/compact-handoff-prompt-v2-20260509-183739`
- DO NOT TOUCH `/Users/kws/source/android/FixThis/`. All commands: `cd <worktree> && ...` or `git -C <worktree>`.

## Phase 0 Files Changed (this phase)

- **Source code:** `fixthis-compose-core/src/main/kotlin/io/beyondwin/fixthis/compose/core/source/SourceMatcher.kt` — Task 5 (matcher fix: SourceCandidate.scoreMargin semantic now raw score difference)
- **Tests:** `fixthis-compose-core/src/test/kotlin/io/beyondwin/fixthis/compose/core/source/SourceMatcherTest.kt` — 3 new tests added; 2 existing updated
- **Docs/Changelog:** `CHANGELOG.md` (Task 6 seed entry), `docs/superpowers/plans/2026-05-09-compact-handoff-prompt-v2.md` (inventory notes added by Tasks 1-5)

## Docs Scope (only update what's affected by Phase 0)

- `CHANGELOG.md` — verify the v2 seed entry (added by Task 6) accurately reflects what shipped in Phase 0. Phase 0's only user-visible behavior change is the matcher's `scoreMargin` semantic. The seed entry is generic ("Compact feedback handoff prompt v2 (clarity, multi-candidate, instance disambiguation)") and is fine as a phase-0 placeholder. **You may leave it as-is** — Task 7.3 will replace it with the final wording. Do NOT add a duplicate entry.
- `README.md` — Phase 0 has no user-facing feature changes that warrant a README update (the matcher fix is internal). Do NOT modify.
- `docs/mcp.md` — Task 7.2 will document the `scoreMargin` field. Do NOT modify here (will be done in Phase 7).
- `docs/feedback-console-contract.md` — Phase 0 doesn't change the contract. Do NOT modify here (Phase 7 task).
- `docs/operator-runbook.md` (if exists) — Phase 0 has no operational change. Do NOT modify.

## Instructions

1. Read `CHANGELOG.md` and confirm the seed entry exists under `## Unreleased / ### Changed`.
2. If the seed is missing or duplicated: add/dedupe.
3. Otherwise: report DONE with a "no further action needed" note. This is an acceptable outcome for Phase 0 — the meaningful docs deltas (the inventory notes) live in the plan file (orchestrator-managed), and the CHANGELOG seed is intentionally minimal.
4. Commit only if you actually changed a file. Otherwise produce DONE with `commit: null` and `files_updated: []`.

If you do commit:
```
docs(handoff-v2-phase0): finalize Phase 0 docs updates

(brief description)
```

## Result File

Write to: `/Users/kws/source/android/worktrees/compact-handoff-prompt-v2-20260509-183739/.orchestrator/docs_results/phase_0.json`

Schema:
```json
{
  "status": "DONE",
  "summary": "<≤2 sentences>",
  "files_updated": [{"path": "<file>", "change": "<one sentence>"}],
  "commit": "<hash or null>"
}
```

If ESCALATE: set `"status": "ESCALATE"`, add `"escalation": {"blocker": "<one sentence>"}`.
After writing, print contents to stdout.

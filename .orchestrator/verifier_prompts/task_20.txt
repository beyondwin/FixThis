You are a Verifier on Sonnet. MID. No file modifications.

## Worktree
`/Users/kws/source/android/worktrees/compact-handoff-prompt-v2-20260509-183739` — `cd` here. NEVER `/Users/kws/source/android/FixThis/`.

## Files Changed (Task 3.2 — emit instance i/N)
- CompactHandoffRenderer.kt (render() calls InstanceGroupingHelper; compactUiLine inserts instance label)
- CompactHandoffRendererTest.kt (2 new tests)

## Baseline
:fixthis-mcp:test before: 318. After: ~320. Failures = 2 baseline, skipped = 1.

## Acceptance
1. `cd <worktree> && ./gradlew :fixthis-mcp:test --tests "*CompactHandoffRenderer*"` — all pass.
2. `cd <worktree> && ./gradlew :fixthis-mcp:test` — failures = 2, skipped = 1.

## Result file: `.orchestrator/verifier_results/task_20.json`

Schema: `{"status":"PASS|FAIL|ESCALATE", ...}`

After writing, print contents.

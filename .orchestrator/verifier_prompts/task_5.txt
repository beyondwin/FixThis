You are a Verifier sub-agent on Sonnet. Run tests calibrated to the risk level. Do not modify any implementation files.

## Working-Directory Enforcement (CRITICAL)
- Worktree (your only working tree): `/Users/kws/source/android/worktrees/compact-handoff-prompt-v2-20260509-183739`
- DO NOT TOUCH `/Users/kws/source/android/FixThis/` (main checkout). Never `cd` into it. Never run gradle there. Read-only inspection of files is fine if needed for cross-reference, but do not write or commit anywhere.
- All commands: `cd /Users/kws/source/android/worktrees/compact-handoff-prompt-v2-20260509-183739 && ...`

## Risk Level: MID

## Files Changed (in worktree)
- fixthis-compose-core/src/main/kotlin/io/beyondwin/fixthis/compose/core/source/SourceMatcher.kt
- fixthis-compose-core/src/test/kotlin/io/beyondwin/fixthis/compose/core/source/SourceMatcherTest.kt
- docs/superpowers/plans/2026-05-09-compact-handoff-prompt-v2.md

## Baseline (do NOT introduce new failures beyond this)

- **Passing:** 281
- **Failing:** 2 (pre-existing)
- **Pre-existing failures (in `:fixthis-mcp`):**
  1. `FeedbackConsoleServerTest.consoleHtmlUsesModeAwareStudioInspector`
  2. `FeedbackConsoleServerTest.consoleHtmlAnnotationSaveUsesCurrentSelectionPayload`

**IMPORTANT:** `./gradlew test` exits non-zero because of these 2 pre-existing failures. Compare TEST COUNTS, not exit code. A non-zero exit code is acceptable IFF the failure count is still 2 and the failure names match the pre-existing two.

## Test Command

`./gradlew test` — base command. Use scoped variants per the instructions below.

## Acceptance Criteria (from the plan)

`./gradlew :fixthis-compose-core:test` — must show all `:fixthis-compose-core` tests passing (no failures in this module).

Run this command FIRST. All assertions must hold for it to be PASS.

## Test Instructions (MID — scope by touched modules)

The change touches only the `:fixthis-compose-core` module. The plan's MID instructions are:

1. **Primary check (acceptance criteria):**
   ```bash
   cd /Users/kws/source/android/worktrees/compact-handoff-prompt-v2-20260509-183739 && ./gradlew :fixthis-compose-core:test
   ```
   This must exit 0 and report all tests passing.

2. **Downstream check (no integration regressions):** the matcher's output is consumed by `:fixthis-mcp` (renderers, formatters). Run:
   ```bash
   cd /Users/kws/source/android/worktrees/compact-handoff-prompt-v2-20260509-183739 && ./gradlew :fixthis-mcp:test
   ```
   PASS condition: failure count is exactly 2 and the failure names match the pre-existing pair above. Any new failure (3rd+ failure or any failure with a different name) → FAIL.

3. **Spot-check the new test names exist in test output:**
   - `scoreMarginIsPopulatedOnRankOneWhenRunnerUpExists`
   - `scoreMarginIsNullWhenOnlyOneCandidate`
   - `matchReturnsEmptyListWhenNoCandidates`

   Confirm via grep on test report or via running tests scoped by name:
   ```bash
   cd /Users/kws/source/android/worktrees/compact-handoff-prompt-v2-20260509-183739 && ./gradlew :fixthis-compose-core:test --tests "*SourceMatcher*" --info 2>&1 | grep -E "scoreMargin|matchReturnsEmpty"
   ```

## If Tests Fail — Debugging Checklist
1. Re-run the failing test in isolation
2. Read the full stack trace
3. Real bug vs environment vs setup
4. Missing deps? Run install commands
5. Only ESCALATE if you cannot resolve

## Result File

Write your JSON result to: `/Users/kws/source/android/worktrees/compact-handoff-prompt-v2-20260509-183739/.orchestrator/verifier_results/task_5.json`

Schema:
```json
{
  "status": "PASS",
  "summary": "<≤3 sentences>",
  "risk_level": "MID",
  "tests_run": ["./gradlew :fixthis-compose-core:test", "./gradlew :fixthis-mcp:test"],
  "results": [
    {"suite": "fixthis-compose-core:test", "status": "PASS", "count": 0, "failures": 0},
    {"suite": "fixthis-mcp:test", "status": "PASS", "count": 0, "failures": 2, "failure_names": ["FeedbackConsoleServerTest.consoleHtmlUsesModeAwareStudioInspector", "FeedbackConsoleServerTest.consoleHtmlAnnotationSaveUsesCurrentSelectionPayload"], "note": "matches pre-existing baseline; no new failures"}
  ],
  "issues": ["none"],
  "escalation": null
}
```

If FAIL: set `"status": "FAIL"` and populate `issues` with concrete failure descriptions (test name, error, file:line).
If ESCALATE: set `"status": "ESCALATE"` and populate `escalation` with type/blocker/attempted/cause/options.

After writing the file, print its contents to stdout.
